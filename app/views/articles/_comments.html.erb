<% @article.comments.each do |comment| %>
    <div class="comments">
        <p><%= comment.body %></p>
        <p><%= comment.user.username %></p>
        <p class="small"> Added <%= time_ago_in_words(comment.created_at) %> ago</p>
    </div>
<% end %>